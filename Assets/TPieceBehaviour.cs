using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TPieceBehaviour : MonoBehaviour
{
    public GameObject tetrimino;
    /*private controls controls;
    

    private bool isControlled = true;
    private float xPosition;

    private void Start()
    {
        
    }

    void FixedUpdate()
    {
        if(isControlled)
        {
            xPosition = transform.position.x;
            xPosition = Mathf.Round(xPosition);
            sendToControls(xPosition);
        }
    }

    private void sendToControls(float xPosition)
    {
        controls controls = tetrimino.gameObject.GetComponent<controls>();
        controls.GetTPieceX(xPosition);
    }*/
}
